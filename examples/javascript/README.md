# JavaScript example

Modern browser JavaScript. Import `check(file)` from `examples/javascript/number_checker.js`; it calls the same-origin backend proxy. Set `NUMBERCHECKER_API_KEY` only on that proxy server; the browser must never hold the key.