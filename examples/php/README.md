# Php example

PHP 8+ with cURL. From the repository root run `php examples/php/example.php` (it reads `examples/numbers.txt`).
Set `NUMBERCHECKER_API_KEY` to a real key only in your local environment.
