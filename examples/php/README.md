# Php example

PHP 8+ with cURL. From the repository root run `php examples/php/number_checker.php` (it reads `examples/numbers.txt`).
Set `NUMBERCHECKER_API_KEY` to a real key only in your local environment.
