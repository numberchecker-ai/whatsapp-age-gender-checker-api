# Nodejs example

Node.js 18+. From the repository root run `node examples/nodejs/example.js` (it reads `examples/numbers.txt`).
Set `NUMBERCHECKER_API_KEY` to a real key only in your local environment.
