# Shell example

Bash, curl, and jq. From the repository root run `bash examples/shell/number_checker.sh examples/numbers.txt`.
Set `NUMBERCHECKER_API_KEY` to a real key only in your local environment.
