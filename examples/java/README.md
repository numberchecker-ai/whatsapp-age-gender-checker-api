# Java example

Java 11+. From the repository root run `javac examples/java/NumberChecker.java && java -cp examples/java NumberChecker` (it reads `examples/numbers.txt`).
Set `NUMBERCHECKER_API_KEY` to a real key only in your local environment.
