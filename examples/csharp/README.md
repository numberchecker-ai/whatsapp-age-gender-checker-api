# Csharp example

.NET 6+. From the repository root run `dotnet run --project examples/csharp` (it reads `examples/numbers.txt`).
Set `NUMBERCHECKER_API_KEY` to a real key only in your local environment.
