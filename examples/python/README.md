# Python example

Python 3.10+ with `requests`. From the repository root run `pip install requests && python examples/python/example.py`.
Set `NUMBERCHECKER_API_KEY` to a real key only in your local environment.
